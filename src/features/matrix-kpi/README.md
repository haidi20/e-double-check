# Matrix KPI

Feature untuk menampilkan metrik KPI operasional distribusi.

Struktur folder mengikuti pola feature lain:

- `screen/` untuk komponen tampilan
- `type/` untuk tipe data
- `vm/` untuk state dan view model
